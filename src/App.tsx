import { useEffect,useRef,useState } from 'react'
import { CalendarDays,ChefHat,Heart,MoreHorizontal,Plus,X,Download,Upload,UserRound,ShieldCheck,Leaf,ImageOff,PenLine } from 'lucide-react'
import { downloadBackup,importBackup,readBackupFile } from './storage/backup'
import { getSetting,setSetting } from './storage/db'
import { deleteRecipe,listRecipes,markCooked,saveRecipe,toggleFavorite } from './storage/recipes'
import type { Recipe,RecipeDraft } from './domain/recipe'
import { RecipeLibrary } from './components/RecipeLibrary'
import { RecipeDetail } from './components/RecipeDetail'
import { RecipeEditor } from './components/RecipeEditor'
type Page='week'|'recipes'|'favorites'|'more';type View={kind:'list'}|{kind:'detail';recipe:Recipe}|{kind:'editor';recipe?:Recipe}
const nav=[['week','Vecka',CalendarDays],['recipes','Recept',ChefHat],['add','Lägg till',Plus],['favorites','Favoriter',Heart],['more','Mer',MoreHorizontal]] as const
function WeekPage(){return <main className="page"><p className="eyebrow">Din matvecka</p><h1>Vecka</h1><section className="hero-card"><div className="hero-icon"><Leaf/></div><h2>Din nästa matvecka börjar här.</h2><p>Snart kan du samla veckans måltider på ett lugnt och överskådligt sätt.</p><div className="coming"><span/> Kommer snart</div></section><section className="tip-card"><span>En god vana i taget</span><p>Meal Prep sparar all din personliga information lokalt på den här enheten.</p></section></main>}
function MorePage({onRestored}:{onRestored:()=>void}){const [name,setName]=useState(''),[status,setStatus]=useState('');const input=useRef<HTMLInputElement>(null);useEffect(()=>{void getSetting<string>('profileName').then(v=>setName(v??''))},[]);async function save(){await setSetting('profileName',name.trim());setStatus('Namnet är sparat lokalt.')}async function restore(file?:File){if(!file)return;try{const backup=await readBackupFile(file);if(!confirm('Återställningen ersätter all befintlig lokal Meal Prep-data. Vill du fortsätta?'))return;await importBackup(backup);setName(await getSetting<string>('profileName')??'');onRestored();setStatus('Backupen har återställts.')}catch(e){setStatus(e instanceof Error?e.message:'Backupen kunde inte läsas.')}finally{if(input.current)input.current.value=''}}return <main className="page"><p className="eyebrow">Inställningar</p><h1>Mer</h1><section className="settings-card"><Heading icon={<UserRound/>} title="Min profil" text="Testa att lokal lagring fungerar."/><label htmlFor="name">Namn</label><input id="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Ditt namn"/><button className="primary" onClick={save}>Spara</button></section><section className="settings-card"><Heading icon={<ShieldCheck/>} title="Data & backup" text="Spara en trygg kopia av all lokal data."/><button className="secondary" onClick={()=>void downloadBackup()}><Download/>Exportera backup</button><button className="secondary" onClick={()=>input.current?.click()}><Upload/>Återställ backup</button><input ref={input} className="file-input" type="file" accept="application/json,.json" onChange={e=>void restore(e.target.files?.[0])}/>{status?<p className="status" role="status">{status}</p>:null}</section></main>}
function Heading({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="section-heading"><span className="small-icon">{icon}</span><div><h2>{title}</h2><p>{text}</p></div></div>}
export default function App(){const [page,setPage]=useState<Page>(()=>{const p=location.hash.slice(1);return ['week','recipes','favorites','more'].includes(p)?p as Page:'week'}),[view,setView]=useState<View>({kind:'list'}),[recipes,setRecipes]=useState<Recipe[]>([]),[sheet,setSheet]=useState(false),[toast,setToast]=useState('');async function refresh(){setRecipes(await listRecipes())}useEffect(()=>{void refresh()},[]);function go(next:Page){setPage(next);setView({kind:'list'});location.hash=next}function create(){setSheet(false);setPage('recipes');setView({kind:'editor'})}async function store(draft:RecipeDraft){const recipe=await saveRecipe(draft);await refresh();setView({kind:'detail',recipe});setPage('recipes')}async function mutate(action:()=>Promise<Recipe>,message?:string){const recipe=await action();await refresh();setView({kind:'detail',recipe});if(message){setToast(message);setTimeout(()=>setToast(''),2400)}}async function remove(recipe:Recipe){if(!confirm(`Ta bort recept?\n\nDet här tar bort ${recipe.title} från din lokala receptbank.`))return;await deleteRecipe(recipe.id);await refresh();setView({kind:'list'});setPage('recipes')}const content=view.kind==='editor'?<RecipeEditor initial={view.recipe} titles={recipes.map(r=>r.title)} onSave={store} onCancel={()=>view.recipe?setView({kind:'detail',recipe:view.recipe}):setView({kind:'list'})}/>:view.kind==='detail'?<RecipeDetail recipe={view.recipe} onBack={()=>setView({kind:'list'})} onEdit={()=>setView({kind:'editor',recipe:view.recipe})} onFavorite={()=>void mutate(()=>toggleFavorite(view.recipe.id))} onCook={()=>void mutate(()=>markCooked(view.recipe.id),'Markerat som lagat')} onDelete={()=>void remove(view.recipe)}/>:page==='recipes'?<RecipeLibrary recipes={recipes} onOpen={recipe=>setView({kind:'detail',recipe})} onCreate={()=>setSheet(true)}/>:page==='favorites'?<RecipeLibrary recipes={recipes} favoritesOnly onOpen={recipe=>setView({kind:'detail',recipe})} onCreate={()=>setSheet(true)}/>:page==='more'?<MorePage onRestored={()=>void refresh()}/>:<WeekPage/>;return <div className="app-shell"><header><div className="brand"><span><ChefHat/></span><strong>Meal Prep</strong></div><span className="local-pill">Lokalt & privat</span></header>{content}{view.kind!=='editor'&&view.kind!=='detail'?<nav aria-label="Huvudnavigation">{nav.map(([id,label,Icon])=>id==='add'?<button key={id} className="add-button" aria-label={label} onClick={()=>setSheet(true)}><span><Icon/></span><small>Lägg till</small></button>:<button key={id} className={page===id?'active':''} aria-current={page===id?'page':undefined} onClick={()=>go(id)}><Icon/><small>{label}</small></button>)}</nav>:null}{sheet?<div className="overlay" onClick={()=>setSheet(false)}><section className="sheet add-sheet" role="dialog" aria-modal="true" aria-labelledby="sheet-title" onClick={e=>e.stopPropagation()}><div className="handle"/><button className="close" aria-label="Stäng" onClick={()=>setSheet(false)}><X/></button><span className="sheet-icon"><Plus/></span><h2 id="sheet-title">Lägg till recept</h2><p>Hur vill du lägga till receptet?</p><button className="sheet-option" onClick={create}><PenLine/><span><b>Skapa manuellt</b><small>Fyll i receptet själv</small></span></button><button className="sheet-option disabled" disabled><ImageOff/><span><b>Importera från bilder</b><small>Kommer i nästa steg</small></span></button></section></div>:null}{toast?<div className="toast" role="status">{toast}</div>:null}</div>}
