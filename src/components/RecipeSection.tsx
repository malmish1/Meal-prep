import type {Recipe} from "../domain/recipe";import {RecipeLibrary} from "./RecipeLibrary";
export function RecipeSection({recipes,onOpen,onCreate}:{recipes:Recipe[];onOpen:(r:Recipe)=>void;onCreate:()=>void;onSaved?:()=>Promise<void>}){return <><div className="recipe-tabs" aria-label="Receptkälla"><strong>Mina recept</strong></div><RecipeLibrary recipes={recipes} onOpen={onOpen} onCreate={onCreate}/></>}
