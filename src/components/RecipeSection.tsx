import {useState} from "react";import type {Recipe} from "../domain/recipe";import {RecipeLibrary} from "./RecipeLibrary";import {RecipeDiscovery} from "./RecipeDiscovery";
export function RecipeSection({recipes,onOpen,onCreate,onSaved}:{recipes:Recipe[];onOpen:(r:Recipe)=>void;onCreate:()=>void;onSaved:()=>Promise<void>}){const[tab,setTab]=useState<"mine"|"discover">("mine");return <><div className="recipe-tabs" role="tablist" aria-label="Receptkälla"><button role="tab" aria-selected={tab==="mine"} onClick={()=>setTab("mine")}>Mina recept</button><button role="tab" aria-selected={tab==="discover"} onClick={()=>setTab("discover")}>Upptäck</button></div>{tab==="mine"?<RecipeLibrary recipes={recipes} onOpen={onOpen} onCreate={onCreate}/>:<RecipeDiscovery recipes={recipes} onSaved={onSaved}/>}</>}
