import{describe,expect,it}from"vitest";import{buildSearchStages,dedupeCandidates,normalizeSearchIntent}from"./searchStrategy";
describe("search escalation",()=>{it("normalizes common chicken fillet spellings",()=>{expect(normalizeSearchIntent("Kycklingfile")).toBe(normalizeSearchIntent("Kycklingfilé"));expect(normalizeSearchIntent("kyckling file")).toBe(normalizeSearchIntent("Kycklingfilé"))});it("broadens before international fallback",()=>{const stages=buildSearchStages("Kycklingfilé");expect(stages).toHaveLength(6);expect(stages.some(x=>x.name==="breddad svensk")).toBe(true);expect(stages.at(-1)?.name).toBe("internationell bred")})});
describe("duplicate protection",()=>{it("rejects same title or same URL as filler",()=>{const base={title:"Kyckling bowl",sourceUrl:"https://a.se/recept",id:"1"}as any;expect(dedupeCandidates([base,{...base,id:"2",sourceUrl:"https://b.se/annat"},{...base,id:"3",title:"Annan rätt"}])).toHaveLength(1)})});
