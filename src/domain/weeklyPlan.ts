import type{DietPhase,InventoryItem,StandardMeal}from"./personalization";import type{PromotionItem}from"./promotion";import type{Recipe}from"./recipe";
export type MealCategory="breakfast"|"lunch"|"dinner"|"snack";export interface WeeklyRecommendation{id:string;category:MealCategory;title:string;sourceName:string;sourceUrl:string;sourceType:"personal"|"external"|"standard";calories:number|null;protein:number|null;carbs:number|null;fat:number|null;servings:number|null;ingredients:Array<{name:string;quantity:string;unit:string}>;mealPrepSuitability?:string;primaryProtein:string;cuisine:string;favorite:boolean;score:number;reasons:string[];standard?:boolean}
export interface ShoppingItem{displayName:string;normalizedName:string;quantity:number|null;unit:string|null;homeQuantity:number|null;warning?:string;promotion?:{displayName:string;price:number|null;priceType:string}}
export interface WeeklyPlan{id:string;createdAt:string;dietPhaseSnapshot:DietPhase;calorieTargetSnapshot:number|null;activeFlyerId?:string;recommendations:Record<MealCategory,WeeklyRecommendation[]>;selections:Partial<Record<MealCategory,WeeklyRecommendation>>;shoppingList:ShoppingItem[];status:"draft"|"finalized"}
export const SCORE_WEIGHTS={favorite:22,rating:2.2,proteinHigh:.35,proteinVeryHigh:.5,mealPrep:10,campaign:7,inventory:9,useFirst:15,swedish:6,recentPenalty:18,calorieDistance:18}as const;
export function isExternalRecipeEligibleForWeeklyRecommendation(x:{sourceType?:string;nutritionEligible?:boolean;caloriesPerServing?:number|null;proteinGramsPerServing?:number|null;carbsGramsPerServing?:number|null;fatGramsPerServing?:number|null;calories?:number|null;protein?:number|null;carbs?:number|null;fat?:number|null;nutritionSource?:string}){const values=[x.caloriesPerServing??x.calories,x.proteinGramsPerServing??x.protein,x.carbsGramsPerServing??x.carbs,x.fatGramsPerServing??x.fat];return x.sourceType==="external"&&x.nutritionEligible===true&&x.nutritionSource==="published"&&values.every(v=>typeof v==="number"&&Number.isFinite(v)&&v>=0&&v<10000)}
const mealType=(r:Recipe,c:MealCategory)=>{const values=r.mealTypes.map(x=>x.toLocaleLowerCase("sv"));return c==="breakfast"?values.some(x=>x.includes("frukost")||x.includes("breakfast")):c==="snack"?values.some(x=>x.includes("mellan")||x.includes("snack")):c==="lunch"?values.some(x=>x.includes("lunch")||x.includes("middag")||x.includes("dinner")):values.some(x=>x.includes("middag")||x.includes("dinner")||x.includes("lunch"))};
export function recipeToRecommendation(r:Recipe,category:MealCategory,context:{dailyCalories:number|null;proteinPriority:string;promotions:PromotionItem[];inventory:InventoryItem[]}):WeeklyRecommendation|null{const complete=[r.caloriesPerServing,r.proteinGramsPerServing,r.carbsGramsPerServing,r.fatGramsPerServing].every(v=>typeof v==="number"&&Number.isFinite(v)&&v>=0);if(!mealType(r,category)||!complete||(r.sourceType==="external"&&!isExternalRecipeEligibleForWeeklyRecommendation(r)))return null;const names=r.ingredients.map(x=>x.name.toLocaleLowerCase("sv")),promotionMatches=context.promotions.filter(p=>!p.ignored&&names.some(n=>n.includes(p.normalizedName)||p.normalizedName.includes(n))),inventoryMatches=context.inventory.filter(i=>names.some(n=>n.includes(i.normalizedName)||i.normalizedName.includes(n))),target=(context.dailyCalories??2500)*(category==="snack"?.14:category==="breakfast"?.2:.25),distance=Math.min(1,Math.abs((r.caloriesPerServing??target)-target)/target);let score=(r.favorite?SCORE_WEIGHTS.favorite:0)+(r.rating??0)*SCORE_WEIGHTS.rating+(r.proteinGramsPerServing??0)*(context.proteinPriority==="veryHigh"?SCORE_WEIGHTS.proteinVeryHigh:context.proteinPriority==="high"?SCORE_WEIGHTS.proteinHigh:.2)+(r.mealPrepSuitability==="excellent"||r.mealPrepSuitability==="very_good"?SCORE_WEIGHTS.mealPrep:0)+promotionMatches.length*SCORE_WEIGHTS.campaign+inventoryMatches.length*SCORE_WEIGHTS.inventory+inventoryMatches.filter(x=>x.useFirst).length*SCORE_WEIGHTS.useFirst-distance*SCORE_WEIGHTS.calorieDistance;if(r.lastCookedAt&&Date.now()-new Date(r.lastCookedAt).getTime()<14*86400000)score-=SCORE_WEIGHTS.recentPenalty;const reasons=[r.favorite?"❤️ Ett av dina favoritrecept":"",inventoryMatches[0]?`🏠 Du har ${inventoryMatches[0].displayName} hemma`:"",promotionMatches[0]?`🔥 Matchar ${promotionMatches[0].displayName} på kampanj`:"",`${r.proteinGramsPerServing} g protein`,r.mealPrepSuitability?"🍱 Bra för matlådor":""].filter(Boolean).slice(0,3);return{id:r.id,category,title:r.title,sourceName:r.sourceType==="external"?r.sourceName:"Mitt recept",sourceUrl:r.sourceUrl,sourceType:r.sourceType==="external"?"external":"personal",calories:r.caloriesPerServing!,protein:r.proteinGramsPerServing!,carbs:r.carbsGramsPerServing!,fat:r.fatGramsPerServing!,servings:r.servings??null,ingredients:r.ingredients.map(x=>({name:x.name,quantity:x.quantity,unit:x.unit})),mealPrepSuitability:r.mealPrepSuitability,primaryProtein:r.primaryProtein,cuisine:r.cuisine,favorite:r.favorite,score,reasons}}
export function standardRecommendation(meal:StandardMeal,category:"breakfast"|"snack"):WeeklyRecommendation{return{id:meal.id,category,title:meal.title,sourceName:"Din standard",sourceUrl:"",sourceType:"standard",calories:meal.nutrition.calories,protein:meal.nutrition.protein,carbs:meal.nutrition.carbs,fat:meal.nutrition.fat,servings:1,ingredients:meal.ingredients.map(x=>({name:x.displayName,quantity:x.quantity,unit:x.unit})),primaryProtein:"",cuisine:"",favorite:false,score:999,reasons:["⭐ Din standard"],standard:true}}
export function diverseTop(items:WeeklyRecommendation[],limit:number){const seen=new Set<string>(),proteinCount=new Map<string,number>();return[...items].sort((a,b)=>b.score-a.score).filter(x=>{const key=`${x.title.toLocaleLowerCase("sv")}|${x.sourceUrl}`;if(seen.has(key))return false;const protein=x.primaryProtein.toLocaleLowerCase("sv");if(protein&&(proteinCount.get(protein)??0)>=3)return false;seen.add(key);proteinCount.set(protein,(proteinCount.get(protein)??0)+1);return true}).slice(0,limit)}
