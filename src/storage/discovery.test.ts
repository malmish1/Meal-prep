import{beforeEach,describe,expect,it}from"vitest";import type{ExternalRecipeCandidate}from"../domain/externalRecipe";import{candidateToRecipe,hasCompletePublishedNutrition}from"../domain/externalRecipe";import{resetDBConnectionForTests}from"./db";import{dismissCandidate,getCachedDiscovery,saveDiscovery}from"./discovery";import{filterRecipes,listRecipes,saveRecipe,toggleFavorite}from"./recipes";
beforeEach(async()=>{await resetDBConnectionForTests();await new Promise<void>((resolve,reject)=>{const request=indexedDB.deleteDatabase("meal-prep");request.onsuccess=()=>resolve();request.onerror=()=>reject(request.error)})});
function candidate(sourceUrl="https://www.ica.se/recept/kycklinggryta"):ExternalRecipeCandidate{return{id:"external-1",title:"Kycklinggryta",description:"En gryta",sourceName:"ICA",sourceUrl,primaryProtein:"Kyckling",mealTypes:["Middag"],cuisine:null,estimatedPrepTime:30,servings:4,caloriesPerServing:600,proteinPerServing:45,carbsPerServing:60,fatPerServing:18,nutritionSource:"published",nutritionConfidence:.95,nutritionEligible:true,swedishIngredientAvailability:"excellent",ingredientNames:["kyckling"],ingredients:[{name:"Kyckling",quantity:"500",unit:"g"}],instructions:["Tillaga."],tags:["Meal prep"],mealPrepSuitability:"very_good",sourceEvidence:"Receptsidan",nutritionEvidence:"explicit",metadataEvidence:"extracted",confidence:.9,warnings:[],searchTerms:["kyckling"],discoveredAt:new Date().toISOString(),savedToLibrary:false,dismissed:false}}
describe("external recipe discovery",()=>{it("släpper aldrig igenom recept utan kompletta publicerade makron",async()=>{const incomplete={...candidate(),proteinPerServing:null,nutritionEligible:false};expect(hasCompletePublishedNutrition(incomplete)).toBe(false);const saved=await saveDiscovery("kyckling",[incomplete]);expect(saved.candidates).toHaveLength(0);expect((await getCachedDiscovery("kyckling"))?.candidates).toHaveLength(0)});it("mappar kompletta makron och källa",()=>{expect(candidateToRecipe(candidate())).toMatchObject({sourceName:"ICA",nutritionEligible:true,caloriesPerServing:600,proteinGramsPerServing:45,carbsGramsPerServing:60,fatGramsPerServing:18})});it("sparas inte som favorit men kan favoritmarkeras",async()=>{const saved=await saveRecipe(candidateToRecipe(candidate()));expect(saved.favorite).toBe(false);await toggleFavorite(saved.id);expect(filterRecipes(await listRecipes(),"",{favoriteOnly:true})).toHaveLength(1)});it("återanvänder cache och deduplicerar URL",async()=>{await saveDiscovery("kyckling + paprika",[candidate(),{...candidate(),id:"external-2"}]);expect((await getCachedDiscovery("kyckling paprika"))?.candidates).toHaveLength(1)});it("visar inte avfärdade kandidater igen",async()=>{await saveDiscovery("kyckling",[candidate()]);await dismissCandidate("external-1");expect((await getCachedDiscovery("kyckling"))?.candidates).toHaveLength(0)})});
